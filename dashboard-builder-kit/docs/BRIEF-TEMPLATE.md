# Project Brief

Fill this in, then save as `docs/brief.md` (remove `-TEMPLATE`). This is the only manual step — every prompt after this reads from this file.

## Product
- **Industry / sub-sector:**
- **Product or company name (or "auto-generate"):**
- **Product type (e.g., B2B SaaS analytics platform):**
- **Business model (subscription, usage-based, etc.):**
- **Geographic market:**
- **Primary users:**
- **Secondary users (admins, execs, external stakeholders):**
- **Primary business problem this solves:**
- **Key decisions users need to make with this data:**

## Data
- **Data domains (e.g., sales, operations, marketing, finance, logistics):**
- **Primary KPIs (or "auto-recommend"):**
- **Data sources (mock only / CSV / JSON / database / APIs / mixed):**
- **Data refresh requirement (batch / near-real-time / real-time / mixed):**
- **Reporting requirements (scheduled exports, PDF, email, etc.):**

## Scope
- **Required applications (responsive web / mobile / marketing site / admin portal):**
- **Required pages (or "auto-recommend"):**
- **Required features (or "auto-recommend"):**
- **Output depth (concept only / design spec / frontend prototype / full-stack blueprint):**

## Brand
- **Brand attributes (3–5 adjectives):**
- **Preferred colors (or "auto-generate"):**
- **Colors/styles to avoid:**
- **Theme requirements (light / dark / both):**

## Technical & Compliance
- **Preferred tech stack constraints, if any (otherwise defer to CLAUDE.md defaults):**
- **Accessibility target (default WCAG 2.2 AA):**
- **Supported languages:**
- **Deployment target:**
- **Authentication requirements:**
- **User roles (e.g., Admin, Analyst, Viewer, Executive):**
- **Integrations needed:**
- **Compliance/regulatory requirements (e.g., SOC 2, HIPAA, GDPR — note if none):**

## Reference
- **Competitors or reference products (or "none"):**
- **Special requirements:**
